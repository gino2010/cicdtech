# Jenkins

公司只在开发环境中做了简单尝试使用，还用的很浅，先列出docker-compose文件，稍后尝试详细功能

The company only made a simple attempt in the development environment, it is also very shallow, first list the docker-compose file, try the detailed function later.

### Plugins

稍后逐一尝试并添加